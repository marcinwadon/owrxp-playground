# Openwebrx+ playground

```
$ owrxp-playground --help

Usage of owrxp-playground:
  -addr string
        openwebrx service address (default "localhost:8073")
  -offset int
        frequency offset (default 0)
  -sq int
        squech level (default -120)
```
